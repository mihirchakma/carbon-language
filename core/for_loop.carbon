package Sample api;

fn Main() -> i32 {
    
    // array
    var names: [String; 5] = ("Mihir", "Amalsha", "Kamal", "Bhagya", "Sehara");
    
    // Note: For loop did not implemented yet well
    // This is a only an example of for loop
    for(var name: String in names) {
        Print(name);
    }

    // Will upload after finish their implemented solution

    return 0;
}